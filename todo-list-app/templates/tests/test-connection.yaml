apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-list-app.fullname" . }}-test-connection"
  labels:
    {{- include "todo-list-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-list-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
